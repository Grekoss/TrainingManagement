{% extends 'baseMain.html.twig' %}

{% block title %}Liste des rapports{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('report') }}
{% endblock %}

{% block content %}
    {{ include('partiels/_messageFlashes.html.twig') }}

    {# la Variable Teacher permet de savoir d'ou l'on vient du menu normal ou du menu Teacher #}
    {% if teacher is not defined %}
        <a href="{{ path('app_report_new')}}" class="btn btn-outline-primary btn-index"><i class="fas fa-plus-circle mr-2"></i>Ajouter un rapport</a>
    {% else %}
        <a href="{{ path('app_teacher')}}" class="btn btn-outline-secondary btn-index">Retour</a>
    {% endif %}

    {{ include('report/partiels/_table.html.twig') }}

{% endblock %}
