{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} text-center p-3" role="alert">
            {{ message|raw }}
        </div>
    {% endfor %}
{% endfor %}
