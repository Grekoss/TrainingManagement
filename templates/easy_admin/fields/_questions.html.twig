<ol>
    {% for question in value %}
        <li>
            <div class="font-weight-bold mr-3"><span class="badge {{ question.level }} mx-2">{{ question.level }}</span>{{ question.question }}</div>
            <div>{{ question.prop1 }}</div>
        </li>
    {% endfor %}
</ol>
